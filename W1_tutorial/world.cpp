#include <iostream>

/*1. Write a C++ program that takes in one user input argument from the command line and checks 
if it is valid hexadecimal number or not, then displays the hexadecimal number to the console.
• If the incorrect number of user input arguments is detected, the program must exit with an 
error
• Hexadecimal values between 0x00 and 0xFF are valid, other values should be rejected
• The letter "x" of number base can either be upper or lower case
• The hexadecimal digits can only contain upper case characters.*/
int main()
{
    std::cout << "Helloworld";
    return 0;
}